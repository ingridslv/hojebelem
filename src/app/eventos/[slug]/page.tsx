import type { Metadata } from "next"; import Image from "next/image"; import Link from "next/link"; import { notFound } from "next/navigation"; import { ArrowLeft, CalendarDays, Clock3, ExternalLink, Heart, MapPin } from "lucide-react"; import { getEvent } from "@/lib/data"; import { formatEventDate,formatEventTime,getEventStatus } from "@/lib/events"; import { ShareButton } from "@/components/share-button";
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const e=await getEvent((await params).slug);if(!e)return {title:"Evento não encontrado"};return {title:e.title,description:e.description.slice(0,150),alternates:{canonical:`/eventos/${e.slug}`},openGraph:{title:e.title,description:e.description.slice(0,150),images:[e.coverImageUrl]}}}
export default async function EventDetail({params}:{params:Promise<{slug:string}>}){const e=await getEvent((await params).slug);if(!e)notFound();return <div className="detail-page"><div className="detail-cover"><Image src={e.coverImageUrl} alt={`Capa de ${e.title}`} fill priority sizes="100vw"/><div className="detail-shade"/><div className="detail-actions shell"><Link href="/eventos" className="circle-action" aria-label="Voltar"><ArrowLeft/></Link><div><ShareButton/><button className="circle-action" aria-label="Favoritar"><Heart/></button></div></div><div className="detail-title shell"><span className="category-badge">{e.category.icon} {e.category.name}</span><h1>{e.title}</h1><p><MapPin/> {e.venueName}, Belém</p></div></div><div className="detail-layout shell"><article className="detail-content"><div className="detail-facts"><div><CalendarDays/><span><small>DATA</small><b>{formatEventDate(e.startAt)} • {getEventStatus(e)}</b></span></div><div><Clock3/><span><small>HORÁRIO</small><b>{formatEventTime(e.startAt)}</b></span></div><div><MapPin/><span><small>LOCAL</small><b>{e.venueName}</b></span></div></div><section><p className="eyebrow">SOBRE O EVENTO</p><h2>Uma experiência para viver Belém</h2><p className="description">{e.description}</p></section><section><p className="eyebrow">ONDE VAI SER</p><h2>{e.venueName}</h2><p>{e.address}</p></section></article><aside className="booking-card"><p>Ingresso</p><div>{e.price==null?<strong>Gratuito</strong>:<><small>a partir de</small><strong>R$ {e.price.toFixed(2).replace(".",",")}</strong></>}</div><a className="gradient-button" href={e.externalPurchaseLink} target="_blank" rel="noopener noreferrer">Reservar agora <ExternalLink/></a>{e.socialLink&&<a className="secondary-button" href={e.socialLink} target="_blank" rel="noopener noreferrer">Ver evento</a>}<small>Você será direcionado para a plataforma do organizador.</small></aside></div><div className="mobile-book"><div><small>Ingresso</small><b>{e.price==null?"Gratuito":`R$ ${e.price.toFixed(2).replace(".",",")}`}</b></div><a href={e.externalPurchaseLink} target="_blank" rel="noopener noreferrer">Reservar agora</a></div></div>}
