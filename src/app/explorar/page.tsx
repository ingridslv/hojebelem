import { CategoryRow } from "@/components/category-row"; import { EventSection } from "@/components/section"; import { getCategories,getEvents } from "@/lib/data";
export default async function Explore(){const [categories,events]=await Promise.all([getCategories(),getEvents()]);return <div className="shell page-space"><div className="page-intro"><p>DESCUBRA BELÉM</p><h1>Qual é a sua vibe?</h1><span>Explore a cidade por tudo que ela tem a oferecer.</span></div><CategoryRow categories={categories}/><EventSection title="Em destaque" events={events.filter(e=>e.isFeatured)}/>{["musica-ao-vivo","gastronomia","teatro"].map(slug=>{const cat=categories.find(c=>c.slug===slug);const filtered=events.filter(e=>e.category.slug===slug);return <EventSection key={slug} title={cat?.name??slug} events={filtered.length?filtered:events.slice(0,3)}/>})}</div>}
