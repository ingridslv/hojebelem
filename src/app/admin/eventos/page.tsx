import Link from "next/link"; import { Plus } from "lucide-react"; import { getEvents } from "@/lib/data";
export default async function AdminEvents(){const events=await getEvents();return <div className="admin-page shell page-space"><div className="admin-heading"><div><p>CONTEÚDO</p><h1>Eventos</h1></div><Link className="gradient-button" href="/admin/eventos/novo"><Plus/> Novo evento</Link></div><div className="admin-list">{events.map(e=><article key={e.id}><span>{e.category.icon}</span><div><b>{e.title}</b><small>{e.venueName} • Publicado</small></div><Link href={`/admin/eventos/${e.id}/editar`}>Editar</Link></article>)}</div></div>}
